name: reactive_mvvm
description: Reactive MVVM Library that facilitates complex state scenarios especially form entry styled after bloc using RxDart.
version: 1.0.4
homepage: https://github.com/Concerti-IO/reactive-mvvm
publish_to: https://pub.dev/reactive_mvvm

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.27.1

dev_dependencies:
  effective_dart: ^1.3.2
  flutter_test:
    sdk: flutter

flutter:
